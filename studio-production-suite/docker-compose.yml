services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xrkr80hd-suite-next
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
    restart: unless-stopped
