services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: studio-production-suite:latest
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
    restart: unless-stopped
